interface Filters{
    brands: string[], 
    processor: string[],
    price: number[],
    connectivity: string[],
    battery: number[],
    usp: string[],
    category: string[]
}

export default Filters;